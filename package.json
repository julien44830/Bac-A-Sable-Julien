{
    "devDependencies": {
        "concurrently": "^9.0.1",
        "lint-staged": "^15.2.10",
        "eslint": "^8.32.0",
        "prettier": "^2.8.0"
    },
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
    },
    "dependencies": {
        "@apollo/server": "^4.11.0"
    },
    "lint-staged": {
        "*.js": [
            "eslint --fix",
            "prettier --write"
        ]
    }
}
